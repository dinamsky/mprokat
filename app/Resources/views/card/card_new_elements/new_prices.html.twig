<div uk-grid>
    <div class="uk-width-1-5">
        Цены <span class="red_star">*</span>
        <div class="help_text">Укажите свои расценки по периодам</div>
    </div>
    <div class="uk-width-3-5">

        <div class="non_plane">
            <div uk-grid>
                <div class="uk-width-1-2">
                    <div class="">
                        <div class="">
                            {% for price in prices if price.id < 3 %}
                                <div class="uk-margin-small-bottom">
                                    <input type="number" name="price[{{ price.id }}]" class="uk-input uk-width-1-3 one_of"> ₽ / <span>{{ price.header }}</span>
                                </div>
                            {% endfor %}
                        </div>
                        <div class="uk-text-small"><span class="red_star">*</span> Одна из расценок: в час/в день обязательна!</div>
                    </div>
                </div>
                <div class="uk-width-1-2">
                    <div id="counted_prices"></div>
                </div>
            </div>

            <div class="uk-margin-top">
                <span class="expander_button" uk-toggle="target: #price_selector">Еще расценки <i uk-icon="icon:chevron-down"></i></span>
            </div>
            <div class="expandable_block uk-margin-top" id="price_selector" hidden>
                {% for price in prices if price.id > 2 and price.id != 6%}
                    <div>
                        <input type="number" name="price[{{ price.id }}]" class="uk-input uk-width-1-3"> ₽ / <span>{{ price.header }} </span>
                    </div>
                {% endfor %}
            </div>
        </div>
        <div class="plane" style="display: none">
            <input type="number" name="price[6]" class="uk-input uk-width-1-3 one_of"> ₽ - <span>Ориентировочная</span>
        </div>


    </div>
    <div class="uk-width-1-5">

    </div>
</div>