<div id="renter_modal_{{ o.id }}" uk-modal="bg-close:false">
    <div class="uk-modal-dialog uk-modal-body">
        <h2 class="uk-modal-title">Анкета для арендатора</h2>
        <button class="uk-modal-close-default" type="button" uk-close></button>

        <div class="uk-margin">
            Оцените владельца по пятибальной шкале в общем,<br> а так же укажите основные детали:
        </div>

        <form action="/user_renter_finish" method="POST">
            <div class="uk-margin uk-flex">
                <span class="uk-margin-small-right">Рейтинг:</span>
                <div class="rating-list">
                    {% for s in [1,2,3,4,5] %}
                    <div class="rating-item filled rs_selector star_{{ s }} {{ s == 5 ? 'stopped' }}" data-star="{{ s }}"><span class="rating-icon rating-icon_active"></span></div>
                    {% endfor %}
                </div>
            </div>

            <input type="hidden" name="rating" value="5">

            <div class="uk-child-width-1-1 uk-child-width-1-2@m uk-margin-bottom" uk-grid>
                <div>
                    <label class="uk-flex-inline uk-flex-middle uk-margin-remove">
                        <input type="checkbox" name="details[]" value="чистота/аккуратность" class="uk-checkbox switch" checked>
                        <span>чистота/аккуратность</span>
                    </label>
                </div>
                <div>
                    <label class="uk-flex-inline uk-flex-middle uk-margin-remove">
                        <input type="checkbox" name="details[]" value="пунктуальность" class="uk-checkbox switch" checked>
                        <span>пунктуальность</span>
                    </label>
                </div>
                <div>
                    <label class="uk-flex-inline uk-flex-middle uk-margin-remove">
                        <input type="checkbox" name="details[]" value="дружелюбность" class="uk-checkbox switch" checked>
                        <span>дружелюбность</span>
                    </label>
                </div>
                <div>
                    <label class="uk-flex-inline uk-flex-middle uk-margin-remove">
                        <input type="checkbox" name="details[]" value="общительность" class="uk-checkbox switch" checked>
                        <span>общительность</span>
                    </label>
                </div>
                <div>
                    <label class="uk-flex-inline uk-flex-middle uk-margin-remove">
                        <input type="checkbox" name="details[]" value="порядочность" class="uk-checkbox switch" checked>
                        <span>порядочность</span>
                    </label>
                </div>
            </div>

            <div class="uk-flex uk-flex-center">
                <button id="renter_finish" class="uk-button uk-button-primary uk-width-1-1 ok_btn">Завершить</button>
            </div>
            <input type="hidden" name="id" value="{{ o.id }}">
        </form>
    </div>
</div>
